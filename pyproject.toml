[project]
name = "impsigil.github.io"
version = "0.1.0"
description = "House of IMP"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mkdocs>=1.6.0",
    "mkdocs-git-revision-date-localized-plugin>=1.5.3",
    "mkdocs-material>=9.5.0",
]
