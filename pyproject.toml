[project]
name = "impsigil.github.io"
version = "0.1.0"
description = "House of IMP"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mkdocs>=1.6.0",
    "mkdocs-material>=9.5.0",
]

[tool.uv]
dev-dependencies = []
